export default {
    methods: {
        /**
         * Delete a specific property form a config
         * @param key
         * @param prop
         */
        deleteConfig(key, prop) {

        }
    }
}